" backupdir
" ====================================================
set nobackup

let backup_dir=expand('/rundata/vim/')

if !isdirectory(backup_dir)
    silent !mkdir -p /rundata/vim
endif

set backupdir=/rundata/vim/

