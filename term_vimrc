" settings for term
" ==================

" term setting not used for gui
if has("gui_running")
	finish
endif

" term color
if &term =~ "xterm"
	let &t_Co=256
	let &t_AF="\e[38;5;%dm"
	let &t_AB="\e[48;5;%dm"
endif 

" cygwin
if has("win32unix")
	"set term=xterm
endif
