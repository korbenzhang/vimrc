" gui running settings.
" ======================

if !has("gui_running")
	finish
endif

"Gui options egmLt
"set guioptions=egm
set go-=T
set go-=r
set go-=L

" Set GUI Font
if has("gui_win32")
	"set guifont=Monaco:h13
	set guifont=Consolas:h13
elseif has("gui_gtk2")
	"set guifont=Bitstream\ Vera\ Sans\ Mono\ 13
	set guifont=Droid\ Sans\ Mono\ 13
	"set guifont=Deja\ Vu\ Sans\ Mono\ 13
	"set guifont=Monaco\ 13
elseif has("gui_macvim")
	"I have no Mac, so i donot know it can work properly or not.
	set guifont=Monaco:h13
end

"noremap <M-Space> :simalt ~<CR>
"inoremap <M-Space> <C-O>:simalt ~<CR>
"cnoremap <M-Space> <C-C>:simalt ~<CR>

" maximize windows when startup
if has("gui_win32")
	au GUIEnter * simalt ~x
endif

" GTK F11 maximum windows
source $HOME/.vim/gui/full_screen_gtk_vimrc

" WinGUI fullscreen
source $HOME/.vim/gui/full_screen_win32_vimrc

" TabLine
set guitablabel=%N.%t
