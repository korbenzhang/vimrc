" Tagbar
"=====================
" F4: Switch on/off TagList
map <F4> <ESC>:TagbarToggle<CR>
nmap ,tl <ESC>:TagbarToggle<CR>

" if not exist ctags, skip.
if !executable('ctags')
	echo "not install ctags"
    finish
endif

